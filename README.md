# GPUForeman
A multi-GPU pipeline scheduler for generative AI workloads. Controller GPU manages job state, output caching and dynamic worker reallocation across heterogeneous tasks (T2I, I2I, mesh, texture). Prior art disclosure — March 2026.
